import styled from 'styled-components';
import { MenuItem, TextField } from '@mui/material';

export const StyledPaginationSelect = styled(TextField)`
  .MuiOutlinedInput-root {
    width: 70px !important;
  }
`;
